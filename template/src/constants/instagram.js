import { addApiActionTypes } from '~/src/utils/api'

const actionTypes = {
  RESET_INSTAGRAM_WEB_TOPSEARCH: `RESET_INSTAGRAM_WEB_TOPSEARCH`,
}

/* Login/Logout */
addApiActionTypes(actionTypes, `API_INSTAGRAM_WEB_TOPSEARCH`)

export default actionTypes