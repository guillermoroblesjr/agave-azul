// import { addApiActionTypes } from '~/src/utils/api'

const actionTypes = {
  RESET_CAMPAIGNS_FIELD: `RESET_CAMPAIGNS_FIELD`,
  RESET_CAMPAIGNS: `RESET_CAMPAIGNS`,
}

// addApiActionTypes(actionTypes, `API_GET_CAMPAIGNS`)

export default actionTypes